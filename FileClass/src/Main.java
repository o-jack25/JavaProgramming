import java.io.File;


public class Main {
    public static void main(String[] arg){
        //file - abstract representation of file and directory path names
        //import java.io.File
        //create file object with file name as parameter in quotes

        /*File file = new File("SecretMessage.rtf");

        //check to see if a file exists or not
        if(file.exists()){
            System.out.println("That file exists");
        }
        else {
            System.out.println("That file doesn't exists");
        }*
    }
}
