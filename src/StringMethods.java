import java.util.Locale;

public class StringMethods {

    public static void main(String[] args){
       String name = "    Jack";

       //returns boolean value
       //boolean result = name.equalsIgnoreCase("JACK");

        //returns length of string
        //int result = name.length();

        //returns character at index
        //char resultChar = name.charAt(0);

        //returns position(index) of letter
        //int result = name.indexOf("k");

        //returns if empty
        //boolean result = name.isEmpty();

        //puts in all caps
        //String result = name.toUpperCase();

        //puts all in lowercase
        //String result = name.toLowerCase();

        //removes empty space from string
        //String result = name.trim();

        //an old character with a new character
        //String result = name.replace("a","u");
        System.out.println();

    }
}
